<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'PR Advisor', dashboard_index_path, class:'heading navbar-brand'%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse auth pad" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
       
        <li class="nav-item">
            <%= link_to "Home", dashboard_index_path,  class: "nav-link text active" %>
        </li>

        <% if user_signed_in? %>
        <li class="nav-item">
            <%= link_to "Chat", chats_path,  class: "nav-link text active" %>
        </li>

          <li class="nav-item pad">
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete,  class: "nav-link btn btn-outline-success" %>
          </li>

        <% else %>
          <li class="nav-item pad">
            <%= link_to 'Log In', new_user_session_path, class:'nav-link btn btn-outline-success'%>
          </li>
          <li class="nav-item pad">
            <%= link_to 'Sign Up', new_user_registration_path, class:'nav-link btn btn-outline-success'%>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
